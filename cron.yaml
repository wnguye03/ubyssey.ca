cron:
- description: Daily UBC event import
  url: /events/import
  schedule: every 24 hours
  retry_parameters:
      min_backoff_seconds: 10
      max_doublings: 3
- description: Morning Push Notification
  url: /test
  schedule: every 1 mins
  retry_parameters:
    min_backoff_seconds: 2.5
    max_doublings: 5
  timezone: America/Vancouver
